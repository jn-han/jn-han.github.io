"use client";
import React from "react";
import { PageTemplateProps } from "./types";

export function PageTemplate({}: PageTemplateProps) {
  return (
    <section
      id="projects"
      className="flex min-h-screen w-full flex-col items-center justify-between"
    ></section>
  );
}
